{
  "name": "parallax-2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Parallax 2.0 — dual-mind AI console built on a Projects engine (Cloudflare Pages + KV).",
  "scripts": {
    "check": "for f in functions/api/*.js; do node --check \"$f\" && echo \"ok  $f\"; done"
  }
}
